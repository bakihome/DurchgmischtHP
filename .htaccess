
# BEGIN WordPress
# Die Anweisungen (Zeilen) zwischen „BEGIN WordPress“ und „END WordPress“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /DurchgmischtHP/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /DurchgmischtHP/index.php [L]
</IfModule>

# END WordPress
# BEGIN Simply Static
# Die Anweisungen (Zeilen) zwischen „BEGIN Simply Static“ und „END Simply Static“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<Files "C:\MAMP\htdocs\DurchgmischtHP/wp-content/uploads\simply-static\24a7294646e35f571734b6f9468c77fd-debug.txt">
require all denied
require host localhost
</Files>
# END Simply Static